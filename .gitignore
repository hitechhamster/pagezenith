# 绝不提交密钥！
.env
*.env
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# 运行产物
reports/
deliverables/*.json
*.xlsx

# 系统
.DS_Store
Thumbs.db

# 本地开发助手（含机器特定路径）
.claude/launch.json

# 🚨 卡密相关：明文卡号与余额库绝不能进仓库
#    造卡脚本输出的 CSV 里是**可直接使用的明文卡号**，提交 = 白送点数
cards-*.csv
*cards*.csv
data/
*.db
*.db-wal
*.db-shm

# 评测与探针的产物：真 API 跑出来的文章、图片、原始指标。
# 体积大、每次都变、对仓库没价值 —— 结论已经写进 voices.py 的注释里了。
work/

# 静态收款码图片：个人支付身份，公开仓库不放
web/assets/pay/
